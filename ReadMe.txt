Known problems and tasks:

Show current scale in the "static" child control.
Fft view is not linked to wave view
Vertical ruler doesn't draw
Add a static for another 'hole'.

Add file length update in SoundChanged
Add file Expand/shrink functionality to CCopyContext
Add undo/redo operation contexts
Add peak data update
Add "Paste" as insert data

Add mouse wheel support
Add autoscroll during selection
Fix mouse cursor over caret without selection
Fix selection boundary  when direction changed (anchor point)
